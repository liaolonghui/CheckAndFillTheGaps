// css单位有哪些？它们有什么使用场景   
 
// px: 静态布局常用，pc端等大屏幕时使用，是css像素(逻辑像素)。
// rem: 自适应布局常用，在手机端适配时常用，由根元素的font-size决定，1rem=根元素字体大小。
// em：不常用，由父元素的font-size决定，1em=父元素字体大小。
// vw：由视口宽度决定，100vw=视口宽度。
// vh：由视口高度决定，100vh=视口高度。
// %：流式布局常用，又称百分比布局。%根据父元素宽高决定。100%=父元素宽度或高度。


// 还有cm，mm这些也是可以使用的css单位。
